import { CalculatorExpressionPipe } from './calculator-expression.pipe';

describe('CalculatorExpressionPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatorExpressionPipe();
    expect(pipe).toBeTruthy();
  });
});
